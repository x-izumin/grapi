package fs

import (
	newFs "github.com/x-izumin/grapi/pkg/grapicmd/util/fs"
)

// CreateDirIfNotExists creates a directory if it does not exist.
var CreateDirIfNotExists = newFs.CreateDirIfNotExists
